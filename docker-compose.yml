services:
  coach-copilot:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - chroma_data:/app/data/chroma_db
      - ./secrets:/app/secrets:ro
    restart: unless-stopped

volumes:
  chroma_data:
